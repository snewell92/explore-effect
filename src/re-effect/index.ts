import { useEffectSync, useMountEffectPromise } from "./useEffect";

/** ReEffect Module import for top levels */
export const ReEffect = {
  useSync: useEffectSync,
  usePromise: useMountEffectPromise
};
